<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <!--<android.support.constraint.Guideline-->
        <!--android:layout_width="wrap_content"-->
        <!--android:layout_height="wrap_content" -->
        <!--android:orientation="vertical"-->
        <!--/>-->

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_home_banner"
        android:layout_width="0dp"
        android:layout_height="200dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        />

    <TextView
        android:id="@+id/tv_home_banner_position"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="15dp"
        android:layout_marginEnd="@dimen/home_banner_margin_position"
        android:layout_marginBottom="15dp"
        android:paddingLeft="@dimen/home_banner_padding_horizontal_position"
        android:paddingRight="@dimen/home_banner_padding_horizontal_position"
        android:paddingTop="@dimen/home_banner_padding_vertical_position"
        android:paddingBottom="@dimen/home_banner_padding_vertical_position"
        android:background="@drawable/shape_banner_home_position"
        android:textColor="@android:color/white"
        android:textSize="11sp"
        app:layout_constraintRight_toRightOf="@+id/rv_home_banner"
        app:layout_constraintBottom_toBottomOf="@+id/rv_home_banner"
        tools:text="3 / 10"
        />


</android.support.constraint.ConstraintLayout>